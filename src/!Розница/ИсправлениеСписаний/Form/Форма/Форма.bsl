
&НаСервере
Процедура БахНаСервере()
	Об=Объект.СписаниеТоваров.ПолучитьОбъект();
	Для Каждого СтрокаТЧ Из Об.Товары Цикл
		Если СтрокаТЧ.Количество<>СтрокаТЧ.КоличествоУпаковок Тогда
			СтрокаТЧ.Количество=СтрокаТЧ.КоличествоУпаковок;
		КонецЕсли;
	КонецЦикла;
	Об.Записать(РежимЗаписиДокумента.Проведение);
КонецПроцедуры

&НаКлиенте
Процедура Бах(Команда)
	БахНаСервере();
КонецПроцедуры
