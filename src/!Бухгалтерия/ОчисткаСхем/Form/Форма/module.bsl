
Процедура КнопкаВыполнитьНажатие(Кнопка)
	Об=Схема.ПолучитьОбъект();
	МассивДляУдаления=Новый Массив;
	Для Каждого СтрокаТЧ ИЗ Об.Состав Цикл
		Если СтрокаТЧ.Договор1.Владелец=Контрагент или СтрокаТЧ.Договор2.Владелец=Контрагент Тогда
			МассивДляУдаления.Добавить(СтрокаТЧ);
		КонецЕсли;
	КонецЦикла;
	Если МассивДляУдаления.Количество()>0 Тогда
		Для Каждого Эл Из МассивДляУдаления Цикл
			Об.Состав.Удалить(Эл);
		КонецЦикла;
		Об.Записать();
	КонецЕсли;
КонецПроцедуры
