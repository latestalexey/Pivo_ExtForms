
Процедура КнопкаВыполнитьНажатие(Кнопка)
	Запись=Последовательности.ПартионныйУчет.СоздатьНаборЗаписей();
Запись.Отбор.Регистратор.Установить(Объект.Ссылка);
Запись.Прочитать();
Если Запись.Количество()=0 Тогда
	ЗаписьРегистрации=Запись.Добавить();
	ЗаписьРегистрации.Регистратор=Объект.Ссылка;
	ЗаписьРегистрации.Период=Объект.Дата;
	Запись.Записать();
КонецЕсли

КонецПроцедуры
